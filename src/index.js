import React from 'react';
import ReactDOM from 'react-dom/client';

function App() {
  return <div id="re-app"></div>;
}

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
